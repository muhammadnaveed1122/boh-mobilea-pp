/**
 * Moved to `src/components/atoms/Sparkline` — the super-admin dashboard tiles use it too.
 * Re-exported here so existing imports in this feature keep working.
 */
export { Sparkline } from '@/components/atoms/Sparkline';
